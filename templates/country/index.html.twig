{% extends 'base.html.twig' %}

{% block title %}Gestion des pays{% endblock %}

{% block body %}

<section>
    <p></p><h1>Liste des pays</h1>
    <p>Vous pouvez visualiser le détail de chaque contact par le bouton "Détail".</p>
    {% if app.user %}
    <p></p><p>Avec un compte d'administration, vous pouvez :</p>
    <p>- créer un nouveau pays : <a href="{{ path('country_create') }}" class="btn btn-success">Créer</a></p>
    <p>- modifier les info d'un pays par le bouton "Modifier" </p>
    <p>- supprimer un pays par le bouton "Supprimer".</p>
    {% endif %}
    <table class="table table-dark table-striped table-bordered table-hover">
        <tr>
            <thead class="thead-light">
            <th>Détail</th>
            </thead>
        </tr>
        {% for n_country in country %}
            <article>
                <tr>
                    <td> {{ n_country.name  }} </td>
                    <td><a href="{{ path('country_show',{'id': n_country.id}) }}" class="btn btn-success">Détail</a></td>
                    {% if app.user %}
                    <td><a href="{{ path('country_edit',{'id': n_country.id}) }}" class="btn btn-warning">Modifier</a></td>
                    <td><a href="{{ path('country_del',{'id': n_country.id}) }}" class="btn btn-danger" onclick="return confirm('Confirmez la suppression ?')">Supprimer</a></td>
                    {% endif %}
                </tr>
            </article>
        {% endfor %}
    </table>
</section>
{{ knp_pagination_render(country) }}
{% endblock %}