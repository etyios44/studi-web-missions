{% extends 'base.html.twig' %}

{% block title %}Hello {{controller_name}}{% endblock %}

{% block body %}

<table class="table table-dark table-striped table-bordered table-hover">
    <tr>
        <thead class="thead-light">
        <th>Titre</th>
        <th>Détail</th>
        </thead>
    </tr>

        <article>
            <tr>
                <td> {{ typemission.title  }} </td>
                <td> {{ typemission.detail  }} </td>
          </tr>
        </article>

</table>

<a href="{{ path('typemission') }}" class="btn btn-success">Retour</a>
{% endblock %}